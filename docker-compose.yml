services:
  ai-health-navigator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-health-navigator
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
